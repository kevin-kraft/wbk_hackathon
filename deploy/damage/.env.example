# Copy to `.env` and fill in. The API key must NOT be committed.
OPENROUTER_API_KEY=sk-or-...
OPENROUTER_MODEL=anthropic/claude-sonnet-5
# Shared API token guarding POST /inspect. Must match the orchestrator's. Empty = off.
WBK_API_TOKEN=
