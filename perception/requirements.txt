# Consolidated deps installed into the perception container image.
# torch / torchvision come from the CUDA base image (pytorch/pytorch:2.5.1-cuda12.4)
# and are intentionally NOT listed here.
#
# Per-service requirements.txt files (services/*/requirements.txt) mirror these
# for the future case of splitting a service into its own image.

# --- shared / web ---
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
numpy>=1.26
pillow>=10.3
opencv-python-headless>=4.9
python-multipart>=0.0.9
supervisor>=4.2

# --- yolo ---
ultralytics>=8.3.0

# --- locateanything + yolo HF stack ---
# NOTE: transformers is SPLIT. LocateAnything's remote code needs the 4.57.x API
# (5.x changed AutoImageProcessor.register and breaks it). SAM 3 needs 5.x, so it
# runs in its OWN image (see requirements-sam3.txt / Dockerfile.sam3).
transformers==4.57.1
accelerate>=0.30
huggingface-hub>=0.25
peft>=0.11
decord>=0.6
lmdb>=1.4            # LocateAnything-3B remote code imports lmdb

# --- dds (cloud detectors: T-Rex2 / DINO-X / Grounding-DINO / DINO-XSeek) ---
# Pure network client, no torch. Pulls opencv/supervision/requests transitively.
# pycocotools is listed explicitly: the SDK imports it (coco_rle masks) but does
# not declare it. NOTE: the SDK depends on opencv-python (non-headless); libgl1
# is already installed in the Dockerfile so cv2 imports fine.
dds-cloudapi-sdk>=0.5.3
pycocotools>=2.0.7
flask>=3.0          # imported by dds-cloudapi-sdk (tasks/base.py) but undeclared
